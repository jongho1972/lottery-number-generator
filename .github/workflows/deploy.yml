# lottery-number-generator repo의 .github/workflows/deploy.yml 로 복사
# Secrets 필요: VPS_HOST, VPS_USER, VPS_SSH_KEY
name: Deploy to VPS

on:
  push:
    branches: [main]
    paths-ignore:
      - 'data/**'
      - 'cache/**'
      - '*.md'
      - '*.ipynb'
  workflow_dispatch:

concurrency:
  group: deploy-lottery
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and redeploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail
            cd /opt/j-hawk/lottery-number-generator
            git fetch origin main
            git reset --hard origin/main
            cd /opt/j-hawk/deploy
            docker compose --profile phase2 build lottery
            docker compose --profile phase2 up -d lottery
            sleep 10
            docker compose ps lottery

      - name: Healthcheck
        run: |
          for i in {1..6}; do
            if curl -fsS https://lottery.j-hawk.kr/healthz; then
              echo "OK"; exit 0
            fi
            sleep 10
          done
          echo "Healthcheck failed"; exit 1
